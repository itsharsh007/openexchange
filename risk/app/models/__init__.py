"""Model package: price prediction, anomaly detection, and per-account risk scoring.

Each model is a small, self-contained class with explicit ``train``/``fit`` + ``predict``/``score``
methods and no hidden global state, so they can be unit-tested in isolation with synthetic data.
"""
