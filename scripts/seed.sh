#!/usr/bin/env bash
# Seed demo accounts, symbols, and simulated order flow.
# Implemented in Phase 1+ once the engine/gateway expose order endpoints.
set -euo pipefail

echo "[seed] placeholder — will create demo accounts (alice, bob), symbols (AAPL, BTC),"
echo "[seed] and replay a simulated order stream against the gateway."
echo "[seed] Wire this up after Phase 1 (engine) and Phase 2 (gateway) land."
