# Copy to `.env.local` (git-ignored) and adjust per environment.
# These default to the local Go gateway started by `make up`.

# Base URL for REST (order submit/cancel, book snapshot).
VITE_API_BASE=http://localhost:8080

# WebSocket URL for the live trades + book + risk feed.
VITE_WS_URL=ws://localhost:8080/ws
